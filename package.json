{
  "name": "nutrilog",
  "private": true,
  "packageManager": "pnpm@9.14.4",
  "scripts": {
    "dev": "pnpm --filter @nutrilog/web dev",
    "build": "pnpm --filter @nutrilog/shared build && pnpm --filter @nutrilog/api build && pnpm --filter @nutrilog/web build",
    "preview": "pnpm --filter @nutrilog/web preview",
    "lint": "pnpm --filter @nutrilog/web lint",
    "typecheck": "pnpm --filter @nutrilog/shared exec tsc --noEmit && pnpm --filter @nutrilog/web exec tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  }
}
