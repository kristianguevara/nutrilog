/** Visual indicator for HTML `required` fields (red asterisk). */
export function RequiredMark() {
  return (
    <span className="ml-0.5 text-rose-400" aria-hidden="true">
      *
    </span>
  );
}
